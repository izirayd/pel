#module debug
#module print_group
#module print_code

group space  : { " " } = ex, ignore;
group newline : {"a"} = ex, glue;
group symbol : { ";" } = ex, split;
group word : { !space and !symbol and !newline } = ex, glue;

test t : { "abab"};

run;
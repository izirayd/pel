#module debug
#module print_code
#module print_tree

group space  : { " " } = ex, ignore;
group symbol : { ";" } = ex, split;
group number : { "0123456789" } = ex, glue;
group word : { !space and !symbol and !number } = ex, glue;

type int_decl			 : { "int", word, ";" } = ex;

test source_text : { "int apple;" };

run;
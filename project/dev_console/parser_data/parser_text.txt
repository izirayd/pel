#module debug
#module print_code
#module print_tree

group space  : { " " } = ex, ignore;
group symbol : { ";" } = ex, split;
group word : { !space and !symbol } = ex, glue;

type t1 : { "int", ";" } = ex;
type t1 : { "float", ";" } = ex;
type t1 : { "str", ";" } = ex;

test source_text : { "str;" };


run;